# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

The project is on Expo SDK 54 (`expo ~54.0.36` in package.json). Keep this pointer in
sync with the installed SDK — a mismatched doc version describes APIs that do not exist here.
